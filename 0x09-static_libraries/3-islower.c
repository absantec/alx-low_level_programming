#include "main.h"
/**
 * _islower - program checks for lowercase character
 * @c: the character to 
 * Return: 1 if c is lowercase, and o if its not
 */
int islower(int c)
{
	return (c >='a' && c <= 'z');
}
